{
  "name": "@fetchmux/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fetchmux/core": "*",
    "@fetchmux/providers": "*",
    "fastify": "^5.10.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js"
  }
}
